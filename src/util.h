#ifndef __SYS_MONITOR_UTIL_H__
#define __SYS_MONITOR_UTIL_H__

inline int _is_number(const char c);
inline int _is_add_or_sub_flag(const char c);
inline int _is_space_or_newline(const char c);

#endif
